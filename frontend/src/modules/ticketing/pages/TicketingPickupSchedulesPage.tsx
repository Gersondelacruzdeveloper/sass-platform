import TicketingPageShell from "../components/TicketingPageShell";

export default function TicketingPickupSchedulesPage() {
  return (
    <TicketingPageShell
      title="Pickup Schedules"
      subtitle="Configure automatic pickup time by product, hotel, date and zone."
    />
  );
}
