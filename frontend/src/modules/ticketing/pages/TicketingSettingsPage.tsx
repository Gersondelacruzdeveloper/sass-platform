import TicketingPageShell from "../components/TicketingPageShell";

export default function TicketingSettingsPage() {
  return (
    <TicketingPageShell
      title="Settings"
      subtitle="Configure payment modes, currency, module behavior and seller permissions."
    />
  );
}
